---
trigger: always_on
description: Use `python3` to run python scripts
---

My system doesn't have a `python` executable, it has `python3`. So, when running python scripts, always invoke them with `python3`.